<section class="hero is-primary">
  <div class="hero-body">
    <h1 class="title">
      [<%= Rails.env %>] Redis instance: '<%= @instance.name %>'
    </h1>
    <p class="subtitle">
      redis v<%= @redis.info["redis_version"] %>
    </p>
    <p class="subtitle">
      URL: </b><%= @instance.url %>
    </p>
  </div>
</section>
